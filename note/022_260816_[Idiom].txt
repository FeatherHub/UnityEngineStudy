hot path에서는 GC allocation을 눈여겨봅니다

Collider[] hits = Physics.OverlapSphere(...);
OverlapSphere는 배열을 생성합니다

---

private readonly Collider[] _hits = new Collider[32];

int count = Physics.OverlapSphereNonAlloc(
    transform.position,
    5f,
    _hits);

---

총알·VFX·적 같은 반복 생성 객체에는 Unity의 ObjectPool<T>

---

static 상태는 Editor의 Domain Reload 설정과 함께 생각

Enter Play Mode에서 Domain Reload를 꺼두면 
static field 값과 static event handler가 이전 Play session에서 유지됩니다

---

static singleton/system을 만든다면 명시적인 초기화 지점을

[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.SubsystemRegistration)]
private static void ResetStatics()
{
    _instance = null;
}

RuntimeInitializeOnLoadMethod는 
첫 scene 로드 과정과 독립적인 runtime initialization hook을 제공합니다

---

[Header("Movement")]
[Tooltip("Maximum movement speed in meters per second")]
[Range(0f, 1f)]
[Min(0)]
[TextArea(3, 10)]

[HideInInspector]

---

[DefaultExecutionOrder(-100)]

숫자가 작은 script가 먼저 실행됩니다.
이 Attribute는 주의해서 사용

-100  GameBootstrap
   0  일반 MonoBehaviour
 100  LateGameSystem

---

[ContextMenu("Kill")]
[ContextMenu("Spawn Test Enemy")]

---

코드 stripping 방지

[Preserve]
public class SomeReflectedType

---

typeof(Collider)의 반환값은 문자열이 아니라 System.Type 객체

Type type = typeof(Collider);
"이 타입이 무엇인지" 설명하는 런타임 메타데이터

type.Name
type.FullName
type.BaseType
type.IsSubclassOf(typeof(Component));
type.GetMethods();
type.GetProperties();

Reflection의 중심 객체가 System.Type

---

typeof(Collider)
코드에 적힌 타입 자체의 Type을 얻습니다

collider.GetType();
현재 객체의 실제 런타임 타입을 얻습니다

Collider collider = GetComponent<BoxCollider>();

Debug.Log(typeof(Collider));     // UnityEngine.Collider
Debug.Log(collider.GetType());   // UnityEngine.BoxCollider

---

[CreateAssetMenu(
    fileName = "Weapon",
    menuName = "RPG/Weapon")]
public class WeaponData : ScriptableObject

ScriptableObject 타입의 .asset 인스턴스를 Project 창에서 쉽게 만들 수 있게 해줍니다

Project 창 우클릭
Create
└─ RPG
   └─ Weapon

---

[RequireComponent<Collider>]
C# 11부터 generic attribute가 가능해져서 이런 형태의 custom API 설계가 가능해졌습니다

---

Debug.Log("Enemy spawned");
Debug.LogWarning("Target missing");
Debug.LogError("Save failed");

Unity Console과 로그 파일 양쪽으로 들어갑니다

---

Debug.LogWarning(
    "Health component initialized with zero HP",
    this);

Debug.LogError("Target missing", gameObject);

---

Exception은 LogException

Debug.LogException(e, this);

---

invariant 검사

Debug.Assert(_weapon != null, "Weapon reference missing", this);

---

로그 prefix를 정해두기

Debug.Log("[Combat] Attack started");
Debug.Log("[AI] State = Chase");
Debug.Log("[Save] Save completed");

---

개발 로그 wrapper + [Conditional]

public static class GameLog
{
    [Conditional("ENABLE_GAME_LOG")]
    public static void Log(string message)
    {
        UnityEngine.Debug.Log(message);
    }
}

ENABLE_GAME_LOG가 정의된 빌드에서 호출이 포함됩니다
Debug.Log 자체는 release build에서도 남아 있고 호출되면 로그 파일에 기록됩니다

GameLog.Log($"Expensive: {BuildHugeString()}");
Conditional call이 컴파일에서 제외되면 이 argument 계산도 함께 사라집니다.

---

Application.logMessageReceivedThreaded += OnLog;

이쪽은 여러 thread에서 동시에 호출될 수 있으므로 handler를 thread-safe하게 작성해야 합니다. 

---

화면상의 텍스트 overlay는 프로젝트에서 UI Toolkit, uGUI 또는 개발용 IMGUI overlay로 구성

DebugOverlay.Log("HP", hp.ToString());
DebugOverlay.Log("State", state.ToString());

---

UNITY_ASSERTIONS
conditional compilation symbol
컴파일 시점에 켜져 있거나 꺼져 있는 이름표

#if UNITY_ASSERTIONS
Debug.Log("Assertions enabled");
#endif

Debug.Assert()는 UNITY_ASSERTIONS가 정의된 구성에서 동작
해당 symbol이 빠진 컴파일에서는 호출이 효과 없는 형태

---

C#의 [Conditional]

[Conditional("MY_DEBUG")]
static void DebugOnly(string message)

DebugOnly("hello");

---

Coroutine은 enabled = false만으로 멈추지 않습니다

gameObject.SetActive(false);
그 GameObject에 속한 coroutine이 중지됩니다
script가 Destroy되어도 중지됩니다

---

Awake
→ 자기 자신 초기화

Start
→ 다른 객체를 사용하는 초기화

OnEnable
→ 이벤트 연결

OnDisable
→ 이벤트 해제

---

Play Mode가 항상 완전히 새 프로세스처럼 시작된다고 생각하면 문제가 생길 수 있습니다
Unity Editor에는 Domain Reload를 끄는 옵션이 있습니다
이 설정에서는 static 상태와 static event subscriber가 다음 Play Mode까지 유지될 수 있습니다

[AutoStaticsCleanup], [OnEnteringPlayMode], [OnExitingPlayMode] 같은 lifecycle 지원

---

[AutoStaticsCleanup]
public static int Counter;

Domain Reload를 끈 빠른 Play Mode workflow에서 static 상태를 자동으로 초기화

---

enabled는 개별 Behaviour의 실행 스위치입니다.

gameObject.SetActive(false);
GameObject 전체의 local active 상태를 끕니다

해당 GameObject의 renderer, collider, rigidbody, script 등이 비활성화 효과를 받고
붙어 있던 coroutine도 중지됩니다

---

component.enabled = false
→ 이 기능을 잠깐 끈다


gameObject.SetActive(false)
→ 이 GameObject 전체를 계층에서 잠깐 재운다

---

gameObject.activeSelf
gameObject.activeInHierarchy

activeSelf는 자기 자신의 local 설정값이고, 
activeInHierarchy는 부모 상태까지 반영한 실제 활성 상태

---

Transform에는 항상 world와 local 두 좌표계가 따라다닙니다

transform.position
transform.rotation

transform.localPosition
transform.localRotation

---

transform.SetParent(parent);
의 기본 worldPositionStays는 true입니다.

transform.SetParent(parent, false);
→ 새 parent 기준 local transform 관계를 사용합니다.

---

Time.deltaTime은 호출 문맥을 읽습니다

private void Update()
{
    float dt = Time.deltaTime;
}
여기서는 frame delta입니다.

private void FixedUpdate()
{
    float dt = Time.deltaTime;
}
Time.fixedDeltaTime 값을 반환합니다

---

timeScale == 0은 pause 시스템에서 여러 파장을 만듭니다

이 상태에서는 FixedUpdate와 WaitForSeconds 기반 coroutine 진행이 정지합니다
pause menu처럼 현실 시간 기준으로 계속 움직일 시스템은 unscaled time 을 사용

Time.unscaledDeltaTime

---

Layer는 숫자 하나이고 LayerMask는 비트 집합입니다.

mask
00000000 00000000 00000000 00100000
                              ↑ layer 5

---

int layer = 5;
int mask = 1 << layer;
처럼 사용합니다

여러 layer를 physics query 하나에 전달할 수 있게 만든 구조

---

GetComponent
TryGetComponent
GetComponentInChildren
GetComponentInParent

---

즉 스위치의 범위가 다릅니다.

GameObject active
    ↓
객체 묶음 전체의 lifecycle


Component enabled
    ↓
그 기능 하나의 lifecycle

---

yield return null = 다음 frame

---

yield return new WaitForSeconds(2f);
Time.timeScale의 영향을 받습니다

yield return new WaitForSecondsRealtime(2f);
계속 시간을 세고 싶으면

---

Coroutine handle을 저장하는 패턴

    _attackRoutine = StartCoroutine(AttackRoutine());
    StopCoroutine(_attackRoutine);

---

한 개만 실행되도록 하는 idiom

private void StartFade()
{
    if (_fadeRoutine != null)
        StopCoroutine(_fadeRoutine);

    _fadeRoutine = StartCoroutine(FadeRoutine());
}

새 요청이 오면 기존 흐름을 취소하고 새 흐름으로 교체하는 구조입니다.

---

흔한 실수

StartCoroutine(AttackRoutine());

// 나중
StopCoroutine(AttackRoutine());

새로운 IEnumerator를 생성

---

Coroutine의 직렬 실행
자식 coroutine 완료까지 기다리는 패턴

yield return StartCoroutine(AttackRoutine());
yield return StartCoroutine(ReloadRoutine());

---

병렬 실행

StartCoroutine(FadeRoutine());
StartCoroutine(MoveRoutine());

둘은 각자의 yield schedule에 따라 진행됩니다

다만 실제 CPU parallelism을 의미하지는 않습니다. 
Coroutine body의 일반 코드는 main thread에서 실행

---

yield break

Coroutine을 즉시 끝내고 싶을 때
일반 method의 early return에 대응되는 iterator idiom

---

yield return new WaitForFixedUpdate();

다음 fixed update 시점까지 기다립니다
Physics 흐름과 맞추고 싶을 때

---

yield return new WaitForEndOfFrame();

현재 frame에서 Camera와 GUI 렌더링이 끝난 뒤, 실제 화면 표시 직전까지 기다립니다. 
Screenshot이나 framebuffer 읽기 같은 작업에서 자주 등장합니다.

---

yield return new WaitWhile(() => _isAttacking);
yield return new WaitUntil(() => _target != null);
조건이 true가 될 때까지 기다립니다. 
predicate는 매 frame Update 이후, LateUpdate 이전에 검사됩니다.

---

public sealed class WaitForHpZero : CustomYieldInstruction
{
    private readonly Health _health;

    public WaitForHpZero(Health health)
    {
        _health = health;
    }

    public override bool keepWaiting => _health.Hp > 0;
}

CustomYieldInstruction.keepWaiting을 Unity가 매 frame 검사합니다.

yield return new WaitForHpZero(_health);

---

Unity의 비동기 operation 자체도 yield할 수 있습니다.

yield return SceneManager.LoadSceneAsync("Dungeon");
yield return request.SendWebRequest();

---

Unity 6.5는 일반 .NET Task와 함께 Unity 전용 **Awaitable**을 제공합니다
Awaitable을 Task의 Unity 특화 대안으로 권장합니다

IEnumerator Attack()
{
    yield return new WaitForSeconds(1f);

    Fire();
}

async Awaitable AttackAsync()
{
    await Awaitable.WaitForSecondsAsync(1f);

    Fire();
}

---

async / await 자체는 C# 5.0에서 2012년에 추가된 언어 기능
C# 컴파일러가 async state machine으로 변환

Unity 2023.1
UnityEngine.Awaitable 공식 도입
Unity API에 본격적인 await 통합

---

async / await
      ≠
자동 background thread

---

보통 continuation은 main thread에서 진행됩니다

---

Awaitable에는 같은 인스턴스를 두 번 await하면 안 된다는 함정

Unity가 allocation을 줄이기 위해 Awaitable 객체를 pooling하기 때문

---

Unity는 매 프레임 Awaitable 객체를 새로 GC 대상으로 만드는 비용을 줄이기 위해 
완료된 Awaitable을 내부 pool로 돌려보냅니다

---

Awaitable operation = FooAsync();

await operation;
await operation; // exception이나 deadlock을 포함한 undefined behavior가 생길 수 있습니다

---

thread switching
continuation 위치 제어 API

await Awaitable.BackgroundThreadAsync();

var result = HeavyParsing();

await Awaitable.MainThreadAsync();

ApplyToGameObject(result);

---

private async Awaitable FooAsync()
{
    var token = destroyCancellationToken;

    await SomethingAsync(token);
}

---

await SomeAwaitable();

DoNextThing();

SomeAwaitable()이 바로 완료되면 DoNextThing()도 바로 이어질 수 있습니다.

---

Unity 6.3 LTS는 2025년 12월 출시됐고
2027년 12월까지 LTS 지원

6.3에서 UnityWebRequest가 여러 주요 플랫폼에서 HTTP/2를 기본 사용하게 됐습니다

Unity 6.4에서 추가

EntityId 본격 도입
InstanceID deprecation 시작

Unity 6.5에서 추가

UI Toolkit
Panel Renderer
Advanced Text Generator 기본 text system
runtime VisualElement Inspector reference
USS Stats Profiler

---



---
